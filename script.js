/**
 * DocMind AI - Main Popup Entry Point
 * Bootstraps individual popup view controller subcomponents loaded via index.html.
 */
document.addEventListener('DOMContentLoaded', () => {
  new PopupController();
});